package com.kenzan.employee.domain;

/* 
 * Spring Data REST is not used in this version.
 */

/*
import java.util.List;
import org.springframework.data.repository.PagingAndSortingRepository;
//import org.springframework.data.repository.query.Param;
import org.springframework.data.repository.query.*;
import org.springframework.data.rest.core.annotation.RepositoryRestResource;

@RepositoryRestResource(path = "employees", collectionResourceRel = "employees")
public interface EmployeeRESTRepository extends PagingAndSortingRepository<Employee, Long> {
	
	//Create the custom queries. To display them: http://localhost:8080/employees/search	
	List<Employee> findByFirstName(@Param("firstName") String firstName);		

	//List<Employee> findAll();	
	//Hotel findByCityAndName(City city, String name);
	
}
*/